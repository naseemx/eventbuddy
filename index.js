// This is the entry point for the app
// Initialize React Native Reanimated first to avoid initialization errors
import 'react-native-reanimated';

// Import the original entry point
import 'expo-router/entry'; 